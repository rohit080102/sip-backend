const user = require('./user');

module.exports = {
    data: require('./model'),
    course: require('./course'),
    subject: require('./subject'),
    staff: require('./staff'),
    student: require('./student'),


    // quize app

    user: require('./user'),

}
